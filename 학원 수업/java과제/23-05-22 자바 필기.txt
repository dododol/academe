23-05-22 

다형성이 지원되기 위해서는 상속이 먼저 진행되어야함

부모클래스 정의시 자식클래스에서 재정의

자식클래스에서 재정의 안해도 부모클래스에서 상속. 자식클래스에 객체생성되어서 부모타입에할당되어짐

목적
자식클래스 설계시 클래스의 기본골격. 필수로 기능해야할 기능들의 가이드라인을 제시

독립된 클래스들의 관계를 맺어줄 수도 있음

Animal animal = cat;
-> Animal타입의animal부모객체변수에 cat자식클래스변수주소가 저장된다

상속
-부모클래스에서 정의해놓은 멤버변수, 멤버클래스를 자식클래스에서 사용하기위해서 적용

-반드시 부모클래스, 자식클래스가 있어야함.

-부모클래스를 먼저 만들어야 하지만
 부모클래스가 먼저 생성되어버리면 상속과 무관. 
 반드시 자식클래스가 생성되어질때 부모클래스를 만든다(extends사용).

-상속되지 않는것 : 부모클래스의 생성자메소드, 프라이빗으로 선언된 것 (자식클래스에서 사용불가능)
	따라서 디폴트 이상으로 접근지정자를 지정해줘야함

-	Parent() {} -> 기본생성자

- 생성자메소드의 가장 중요한 역할 - 멤버변수 초기화(직접초기화(순위1), 초기화 블럭(순위2), 생성자메소드(순위3)